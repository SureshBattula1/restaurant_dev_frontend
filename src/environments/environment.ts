export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api',
  pusherKey: 'your-pusher-key', // Update with your Pusher key
  pusherCluster: 'ap2', // Update with your Pusher cluster
  pusherEncrypted: true
};

